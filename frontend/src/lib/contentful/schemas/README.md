Contentful schema snapshots used by the frontend.

These files are checked into the repo as a local reference for:
- query planning
- mapper/type updates
- test fixtures and schema review

They are snapshots, not a source of truth. Contentful remains the live source of truth.
